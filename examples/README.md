# Mermaid Flowchart Examples

Run any example with:

```
cargo run -p merdraw -- examples/<file>.mmd
```

All examples target the current parser + layered layout + ASCII renderer.

- http_architecture.mmd: HTTP request/response path (DNS, TCP, TLS, CDN, LB, app, cache, DB).
